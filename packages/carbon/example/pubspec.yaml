name: carbon_example
description: Complete component atlas for the Carbon for Flutter package.
publish_to: "none"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  carbon:
    path: ../
  mix_atlas:
    git:
      url: https://github.com/btwld/mix.git
      ref: 476b3cef650fa4a0e1d4dc950970556bc0fb26cb
      path: packages/mix_atlas

dev_dependencies:
  dart_code_metrics_presets: ^2.32.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
