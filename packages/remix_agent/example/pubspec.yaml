name: remix_agent_example
description: WidgetsApp example for remix_agent. Local stylers only; no theme package.
publish_to: none
resolution: workspace
version: 0.1.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  remix: ^1.0.0-beta.4
  remix_agent: ^0.1.0-beta.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
