/// Marker library for the `remix_cli` package.
///
/// `remix_cli` ships a command-line product, not a programmatic API: the
/// supported surface is the `remix` executable in `bin/remix.dart`. The
/// implementation lives under `lib/src/` and is intentionally not re-exported
/// here, so narrowing or reshaping it is not a breaking change for consumers.
library;
