import 'package:flutter/widgets.dart';
import 'package:remix/remix.dart';

import '../components/transcript.dart';

@immutable
final class {{typePrefix}}AgentTranscriptRecipe {
  const {{typePrefix}}AgentTranscriptRecipe({required this.style});
  final {{typePrefix}}TranscriptStyler style;
}

{{typePrefix}}AgentTranscriptRecipe {{valuePrefix}}AgentTranscriptRecipe({
  {{typePrefix}}TranscriptStyler style = const {{typePrefix}}TranscriptStyler.create(),
}) => {{typePrefix}}AgentTranscriptRecipe(
  style: {{typePrefix}}TranscriptStyler(
    viewport: BoxStyler().padding(.only(right: 12)),
    item: BoxStyler(),
    spacing: 16,
  ).merge(style),
);
